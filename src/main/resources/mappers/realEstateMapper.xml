<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.quiz.lesson03.quiz01.mapper.RealEstateMapper">
	<select id="selectRealEstate" parameterType="int"
		resultType="com.quiz.lesson03.quiz01.domain.RealEstate">
		SELECT
			`id`
			, `realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
			, `rentPrice`
			, `createdAt`
			, `updatedAt`
		FROM
			`real_estate`
		WHERE
			`id` = #{id}
	</select>

	<select id="selectRealEstateList" parameterType="map"
		resultType="com.quiz.lesson03.quiz01.domain.RealEstate">
		SELECT
			`id`
			, `realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
			, `rentPrice`
			, `createdAt`
			, `updatedAt`
		FROM
			`real_estate`
		WHERE
			`area` <![CDATA[>=]]> #{area}
		AND
			`price` <![CDATA[<=]]> #{price}
		ORDER BY
			`updatedAt`
		DESC
	</select>
</mapper>